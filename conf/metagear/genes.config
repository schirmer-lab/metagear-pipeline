params {
    metaphlan_profiles = false
}

process {

    // MSP-specific publishDir blocks (MSPMINER_MSPMINER, MSP_SEQUENCES,
    // MSP_ABUNDANCE, MSP:GTDBTK_CLASSIFYWF, MSP_METAPHLAN_ANNOTATION) moved to
    // conf/metagear/msp.config along with the workflow factor-out.

}
