params {

    module = ""
    subworkflow = ""
    workflow = "setup"

}

process {

    withName: METAPHLAN_MAKEDB {
        cpus = params.max_cpus
        memory = params.max_memory
        time = params.max_time
    }
}
